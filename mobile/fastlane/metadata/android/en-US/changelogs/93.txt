The wipe did not wipe. Both the wipe pin and the settings wipe deleted messages and keys but lost a race on the way out, so the next launch asked for the old pin and showed a fresh identity. The wipe now goes through Android's own clear-data call: everything gone, next launch is onboarding.

The app lock only covered the home screen. Leave from settings or a chat and come back, and the pin never showed until you walked home. Fixed: the lock covers every screen. A screenshot no longer asks for the pin afterwards.

A message to someone who has not added you back could show a tick while it sat at an address they never read. It now says it is waiting for them and keeps trying. A photo stuck at 98% resumes from the missing piece instead of starting over, and the receiving side says paused instead of vanishing.

Sentence case throughout, shorter settings hints, no white flash on the screenshot toggle, open source row copies the link instead of opening a browser.

Also fixed: a light box across the bottom of the speed & privacy and backup screens, disappearing messages losing their timer when queued, a new pin equal to the wipe pin disarming the wipe, edit and delete frames from strangers, declined requests going deaf after a restart, and the @handle sheet not closing after a successful add.

Five wrong pins now hold the pad for thirty seconds, doubling. Request notifications no longer show a stranger's words. Edits queue and retry like messages. Accept, decline and block sit on the requests list. Photos and files send five slices at a time and camera shots are shrunk like gallery picks. The scam shield reads a first message from a group member you never added and marks their bubbles. Adding by handle or by an already-used link now works for everyone, not only the first person. Reset my invite link now really cuts off old links, on every route, and says what it costs. The onion door has limits against junk floods. Claiming a handle froze the app; fixed. Camera photos are upright, the shared card has no stray underlines, the six digit code can be typed in, and the home strip only speaks when the phone cannot send. Relay-mode friends can reach onion-mode friends again, notifications clear when a chat is opened, sent photos show their timer, and the recording bar sits above the keyboard. One screenshot switch applied at the next start, stop a file mid-send, a wallpaper from your photos, and onboarding asks how your messages should travel. Runs on 32-bit phones. The pin pad, the lock screen, onboarding and every sheet scroll on short screens instead of hiding their button under the navigation bar. The first message to someone new shows what it is doing and counts the seconds.
